name: Android CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GH_PAT }}

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Install Android SDK
      uses: android-actions/setup-android@v4

    - name: Install required SDK components
      run: |
        sdkmanager "platform-tools" \
                   "platforms;android-34" \
                   "build-tools;34.0.0" \
                   "cmdline-tools;latest"

    - name: Accept licenses
      run: yes | sdkmanager --licenses

    - name: Setup Gradle (cache disabled)
      uses: gradle/actions/setup-gradle@v6
      with:
        cache-disabled: true

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    # Build step (allowed to fail)
    - name: Build APK
      run: ./gradlew assembleDebug --stacktrace --info
      continue-on-error: true

    # Always generate build log
    - name: Save build output to file
      run: |
        ./gradlew assembleDebug --stacktrace --info > build_output.txt 2>&1
      continue-on-error: true

    # Commit log only on push to main
    - name: Commit build log
      if: github.event_name == 'push'
      run: |
        git config --global user.name "github-actions"
        git config --global user.email "github-actions@github.com"
        git add build_output.txt
        git commit -m "Update build output log" || echo "No changes to commit"
        git push
      env:
        GITHUB_TOKEN: ${{ secrets.GH_PAT }}
